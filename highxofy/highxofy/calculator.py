"""Calculate high x of y.
"""

import pandas as pd


def calculate(df_org: pd.DataFrame) -> pd.DataFrame:
    """Return dataframe contain high x of y result.

    Args:
        df_org (pd.DataFrame): Original data. i.e. historical data.

    Returns:
        pd.DataFrame: dataframe contain high x of y result.
    """
    df = df_org

    return df
